const express = require('express');
const router = express.Router();

// Meeting feature removed. This router is intentionally empty.
// Keeping the file stubbed to avoid import errors if any stale code references it.

module.exports = router;
